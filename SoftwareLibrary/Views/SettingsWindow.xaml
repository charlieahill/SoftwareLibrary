<Window x:Class="SoftwareLibrary.Views.SettingsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="Settings" Height="220" Width="520" WindowStartupLocation="CenterOwner">
    <Grid Margin="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <StackPanel Orientation="Vertical" Grid.Row="0" Margin="0,0,0,8">
            <TextBlock Text="Backup root folder" FontWeight="SemiBold" />
            <StackPanel Orientation="Horizontal" Margin="0,4,0,0">
                <TextBox x:Name="BackupRootText" Width="380" />
                <Button Content="Browse" Width="80" Margin="8,0,0,0" Click="BrowseBackupRoot_Click" />
            </StackPanel>
        </StackPanel>

        <StackPanel Orientation="Vertical" Grid.Row="1" Margin="0,0,0,8">
            <TextBlock Text="Left column width (px)" FontWeight="SemiBold" />
            <TextBox x:Name="LeftWidthText" Width="120" />
        </StackPanel>

        <StackPanel Orientation="Horizontal" Grid.Row="2" HorizontalAlignment="Right">
            <Button Content="Save" Width="100" Margin="8" Click="Save_Click" />
            <Button Content="Cancel" Width="100" Margin="8" Click="Cancel_Click" />
        </StackPanel>
    </Grid>
</Window>